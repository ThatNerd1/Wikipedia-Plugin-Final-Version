# Test-Fixtures

`mediawiki-article.html` und `mediawiki-parse-response.json` sind reduzierte,
lokal gepflegte Beispiele für MediaWiki-Action-API-Antworten. Sie enthalten
bewusst eingestreute XSS-Payloads, um den Sanitizer zu prüfen. Es werden keine
Remote-Ressourcen geladen.
